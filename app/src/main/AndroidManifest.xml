<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rijaldev.myunsiq">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyUnsiq">
        <activity
            android:name=".InfoActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_info"
            android:theme="@style/Theme.MyUnsiq.Fullscreen"></activity>
        <activity android:name=".PesanActivity1" />
        <activity
            android:name=".PesanActivity0"
            android:label="@string/title_activity_pesan"
            android:theme="@style/Theme.MyUnsiq.NoActionBar" />
        <activity android:name=".AktivasiActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>